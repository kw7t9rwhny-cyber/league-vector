(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.LeagueVectorProjectionBenchmarkV02=api;})(typeof globalThis!=='undefined'?globalThis:this,function(){'use strict';
const VERSION='lv-projection-benchmark-v0.2';
const POSITIONS=['QB','RB','WR','TE','DL','LB','DB'];
const TARGETS=Object.freeze({
QB:['attempts','completions','passing_yards','passing_td','interceptions','sacks','carries','rushing_yards','rushing_td'],
RB:['carries','rushing_yards','rushing_td','targets','receptions','receiving_yards','receiving_td'],
WR:['targets','receptions','receiving_yards','receiving_td','carries','rushing_yards','rushing_td'],
TE:['targets','receptions','receiving_yards','receiving_td','carries','rushing_yards','rushing_td'],
DL:['solo_tackles','assisted_tackles','total_tackles','tackles_for_loss','sacks','qb_hits','interceptions','passes_defended','forced_fumbles','fumble_recoveries','defensive_td','safeties'],
LB:['solo_tackles','assisted_tackles','total_tackles','tackles_for_loss','sacks','qb_hits','interceptions','passes_defended','forced_fumbles','fumble_recoveries','defensive_td','safeties'],
DB:['solo_tackles','assisted_tackles','total_tackles','tackles_for_loss','sacks','qb_hits','interceptions','passes_defended','forced_fumbles','fumble_recoveries','defensive_td','safeties']});
const SCORING=Object.freeze({
'LV-PPR':{passing_yards:.04,passing_td:4,interceptions:-2,rushing_yards:.1,rushing_td:6,receptions:1,receiving_yards:.1,receiving_td:6},
'LV-IDP-BALANCED':{solo_tackles:1.5,assisted_tackles:.75,tackles_for_loss:2,sacks:4,qb_hits:1,interceptions:6,passes_defended:1.5,forced_fumbles:3,fumble_recoveries:3,defensive_td:6,safeties:4}});
const REQUIRED=Object.freeze({QB:['passing_yards','passing_td','interceptions','rushing_yards','rushing_td'],RB:['rushing_yards','rushing_td','receptions','receiving_yards','receiving_td'],WR:['rushing_yards','rushing_td','receptions','receiving_yards','receiving_td'],TE:['rushing_yards','rushing_td','receptions','receiving_yards','receiving_td'],DL:Object.keys(SCORING['LV-IDP-BALANCED']),LB:Object.keys(SCORING['LV-IDP-BALANCED']),DB:Object.keys(SCORING['LV-IDP-BALANCED'])});
function n(v){return Number.isFinite(Number(v))?Number(v):null;}function mean(a){return a.length?a.reduce((x,y)=>x+y,0)/a.length:null;}function median(a){if(!a.length)return null;const x=[...a].sort((a,b)=>a-b),m=Math.floor(x.length/2);return x.length%2?x[m]:(x[m-1]+x[m])/2;}function q(a,p){if(!a.length)return null;const x=[...a].sort((a,b)=>a-b),v=(x.length-1)*p,l=Math.floor(v),h=Math.ceil(v);return l===h?x[l]:x[l]+(x[h]-x[l])*(v-l);}function ranks(a){const s=a.map((v,i)=>({v,i})).sort((x,y)=>x.v-y.v),o=Array(a.length);for(let i=0;i<s.length;){let j=i;while(j+1<s.length&&s[j+1].v===s[i].v)j++;const r=(i+j+2)/2;for(let k=i;k<=j;k++)o[s[k].i]=r;i=j+1;}return o;}function corr(a,b){if(a.length<2||a.length!==b.length)return null;const ma=mean(a),mb=mean(b);let z=0,da=0,db=0;for(let i=0;i<a.length;i++){const x=a[i]-ma,y=b[i]-mb;z+=x*y;da+=x*x;db+=y*y;}return da&&db?z/Math.sqrt(da*db):null;}function spearman(a,b){return corr(ranks(a),ranks(b));}
function cell(c){return c&&c.state==='value'&&Number.isFinite(c.value)?c.value:null;}
function ageOn(birthDate,season){if(!birthDate)return null;const d=new Date(birthDate+'T00:00:00Z');if(Number.isNaN(d.getTime()))return null;const ref=new Date(Date.UTC(season,8,1));let age=ref.getUTCFullYear()-d.getUTCFullYear();const md=ref.getUTCMonth()-d.getUTCMonth();if(md<0||(md===0&&ref.getUTCDate()<d.getUTCDate()))age--;return age>=17&&age<60?age:null;}
function aggregatePlayerSeasons(observations,birthDates={}){const m=new Map();for(const row of observations||[]){if(!row?.gsis_id||!Number.isInteger(row.season)||!POSITIONS.includes(row.position_group))continue;const key=`${row.gsis_id}|${row.season}|${row.position_group}`;if(!m.has(key))m.set(key,{league_vector_player_id:row.league_vector_player_id,gsis_id:row.gsis_id,season:row.season,position:row.position_group,teams:new Set(),games:new Set(),totals:{},source:row.source,birth_date:birthDates[row.gsis_id]||null});const r=m.get(key);if(row.team)r.teams.add(row.team);if(Number.isInteger(row.week))r.games.add(row.week);for(const t of TARGETS[row.position_group]||[]){if(t==='total_tackles')continue;const v=cell(row.stats?.[t]);if(v!=null)r.totals[t]=(r.totals[t]||0)+v;}}const out=[];for(const r of m.values()){if(['DL','LB','DB'].includes(r.position)&&Number.isFinite(r.totals.solo_tackles)&&Number.isFinite(r.totals.assisted_tackles))r.totals.total_tackles=r.totals.solo_tackles+r.totals.assisted_tackles;out.push({...r,teams:[...r.teams],games:r.games.size,age:ageOn(r.birth_date,r.season),per_game:Object.fromEntries(Object.entries(r.totals).map(([k,v])=>[k,r.games.size?v/r.games.size:null]))});}return out;}
function index(rows){const m=new Map();for(const r of rows){const a=m.get(r.gsis_id)||[];a.push(r);m.set(r.gsis_id,a);}for(const a of m.values())a.sort((x,y)=>x.season-y.season);return m;}function history(idx,id,season,max=3){return(idx.get(id)||[]).filter(x=>x.season<season).slice(-max).reverse();}
function activity(r){if(!r)return 0;if(r.position==='QB')return r.totals.attempts||0;if(r.position==='RB')return(r.totals.carries||0)+(r.totals.targets||0);if(r.position==='WR'||r.position==='TE')return r.totals.targets||0;return r.totals.total_tackles||0;}
function cohortThresholds(rows,throughSeason){const out={};for(const p of POSITIONS){const vals=rows.filter(r=>r.position===p&&r.season<=throughSeason).map(activity).filter(x=>x>0);out[p]=q(vals,.5)||0;}return out;}
function repeat(h,t){return n(h[0]?.totals?.[t]);}function weighted(h,t,w=[.6,.3,.1]){let a=0,d=0;for(let i=0;i<Math.min(h.length,w.length);i++){const v=n(h[i].totals[t]);if(v!=null){a+=v*w[i];d+=w[i];}}return d?a/d:null;}function perGame(h,t){if(!h[0])return null;const rate=n(h[0].per_game[t]);if(rate==null)return null;const g=mean(h.map(x=>x.games).filter(Number.isFinite));return rate*Math.max(1,Math.min(17,g||h[0].games));}
function features(h,t,targetSeason){if(!h.length)return null;const vals=[n(h[0]?.totals?.[t]),n(h[1]?.totals?.[t]),n(h[2]?.totals?.[t]),n(h[0]?.games),n(h[0]?.age)!=null?n(h[0].age)+1:null];if(vals[0]==null)return null;return vals;}
function impute(trainX,x){const means=[];for(let j=0;j<trainX[0].length;j++){const vals=trainX.map(r=>r[j]).filter(Number.isFinite);means[j]=mean(vals)||0;}return x.map((v,j)=>Number.isFinite(v)?v:means[j]);}
function standardize(X){const mu=X[0].map((_,j)=>mean(X.map(r=>r[j]))||0),sd=X[0].map((_,j)=>{const m=mu[j],v=mean(X.map(r=>(r[j]-m)**2))||0;return Math.sqrt(v)||1;});return{X:X.map(r=>r.map((v,j)=>(v-mu[j])/sd[j])),mu,sd};}
function solve(A,b){const n=A.length,M=A.map((r,i)=>[...r,b[i]]);for(let i=0;i<n;i++){let p=i;for(let j=i+1;j<n;j++)if(Math.abs(M[j][i])>Math.abs(M[p][i]))p=j;[M[i],M[p]]=[M[p],M[i]];if(Math.abs(M[i][i])<1e-10)return null;const d=M[i][i];for(let k=i;k<=n;k++)M[i][k]/=d;for(let j=0;j<n;j++){if(j===i)continue;const f=M[j][i];for(let k=i;k<=n;k++)M[j][k]-=f*M[i][k];}}return M.map(r=>r[n]);}
function fitRidge(X,y,alpha=1){if(X.length<8)return null;const imp=X.map(r=>impute(X,r)),s=standardize(imp),Z=s.X.map(r=>[1,...r]),p=Z[0].length,A=Array.from({length:p},()=>Array(p).fill(0)),b=Array(p).fill(0);for(let i=0;i<Z.length;i++)for(let j=0;j<p;j++){b[j]+=Z[i][j]*y[i];for(let k=0;k<p;k++)A[j][k]+=Z[i][j]*Z[i][k];}for(let j=1;j<p;j++)A[j][j]+=alpha;const beta=solve(A,b);if(!beta)return null;return{x=>0,beta,mu:s.mu,sd:s.sd,predict(x){const v=impute(X,x).map((z,j)=>(z-s.mu[j])/s.sd[j]);return beta[0]+v.reduce((a,z,j)=>a+z*beta[j+1],0);}};}
function trainRows(rows,position,target,beforeSeason){const idx=index(rows),X=[],y=[];for(const r of rows){if(r.position!==position||r.season>=beforeSeason||!Number.isFinite(r.totals[target]))continue;const h=history(idx,r.gsis_id,r.season,3),f=features(h,target,r.season);if(f){X.push(f);y.push(r.totals[target]);}}return{X,y};}
function tuneAlpha(rows,position,target,targetSeason){const alphas=[.1,1,10,100],validation=targetSeason-1;if(validation<=Math.min(...rows.map(r=>r.season)))return 10;const idx=index(rows),actual=rows.filter(r=>r.position===position&&r.season===validation&&Number.isFinite(r.totals[target])),tr=trainRows(rows,position,target,validation);if(tr.X.length<12||!actual.length)return 10;let best={a:10,e:Infinity};for(const a of alphas){const model=fitRidge(tr.X,tr.y,a);if(!model)continue;const errs=[];for(const r of actual){const f=features(history(idx,r.gsis_id,validation,3),target,validation);if(!f)continue;errs.push(Math.abs(model.predict(f)-r.totals[target]));}const e=mean(errs);if(Number.isFinite(e)&&e<best.e)best={a,e};}return best.a;}
function buildPredictions(rows,targetSeason){const idx=index(rows),actuals=rows.filter(r=>r.season===targetSeason),thresholds=cohortThresholds(rows,targetSeason-1),out=[];const ridgeCache=new Map();for(const a of actuals){const h=history(idx,a.gsis_id,targetSeason,3);if(!h.length)continue;for(const t of TARGETS[a.position]||[]){if(!Number.isFinite(a.totals[t]))continue;const key=`${a.position}|${t}`;if(!ridgeCache.has(key)){const alpha=tuneAlpha(rows,a.position,t,targetSeason),tr=trainRows(rows,a.position,t,targetSeason),model=fitRidge(tr.X,tr.y,alpha);ridgeCache.set(key,{alpha,model,n:tr.y.length});}const rc=ridgeCache.get(key),f=features(h,t,targetSeason);const models={repeat_last:repeat(h,t),per_game:perGame(h,t),weighted_603010:weighted(h,t),ridge_v02:rc.model&&f?rc.model.predict(f):null};for(const [model,prediction]of Object.entries(models))if(Number.isFinite(prediction))out.push({gsis_id:a.gsis_id,position:a.position,target:t,target_season:targetSeason,model,prediction,actual:a.totals[t],history_seasons:h.map(x=>x.season),fantasy_relevant:activity(h[0])>=thresholds[a.position],ridge_alpha:model==='ridge_v02'?rc.alpha:null,training_n:model==='ridge_v02'?rc.n:null});}}return out;}
function metrics(rows){if(!rows.length)return{n:0,mae:null,rmse:null,median_ae:null,bias:null,spearman:null};const e=rows.map(r=>r.prediction-r.actual),ae=e.map(Math.abs);return{n:rows.length,mae:mean(ae),rmse:Math.sqrt(mean(e.map(x=>x*x))),median_ae:median(ae),bias:mean(e),spearman:spearman(rows.map(r=>r.actual),rows.map(r=>r.prediction))};}
function groupMetrics(pred){const m=new Map();for(const r of pred)for(const cohort of ['all',r.fantasy_relevant?'fantasy_relevant':null].filter(Boolean)){const k=`${r.position}|${r.target}|${r.model}|${r.target_season}|${cohort}`,a=m.get(k)||[];a.push(r);m.set(k,a);}return[...m.entries()].map(([k,a])=>{const[p,t,model,s,c]=k.split('|');return{position:p,target:t,model,target_season:+s,cohort:c,...metrics(a)};});}
function completeness(predictions){const m=new Map();for(const r of predictions){const k=`${r.gsis_id}|${r.position}|${r.target_season}|${r.model}`,x=m.get(k)||{gsis_id:r.gsis_id,position:r.position,target_season:r.target_season,model:r.model,pred:{},actual:{},fantasy_relevant:r.fantasy_relevant};x.pred[r.target]=r.prediction;x.actual[r.target]=r.actual;m.set(k,x);}return[...m.values()].map(x=>{const req=REQUIRED[x.position]||[],missing=req.filter(k=>!Number.isFinite(x.pred[k])||!Number.isFinite(x.actual[k]));return{...x,status:missing.length?'partial_projection':'complete_projection',missing};});}
function score(totals,position){const rules=['DL','LB','DB'].includes(position)?SCORING['LV-IDP-BALANCED']:SCORING['LV-PPR'];return Object.entries(rules).reduce((a,[k,w])=>a+(Number.isFinite(totals[k])?totals[k]:0)*w,0);}
function fantasyBenchmark(pred){const c=completeness(pred),rows=c.filter(x=>x.status==='complete_projection').map(x=>({...x,predicted_points:score(x.pred,x.position),actual_points:score(x.actual,x.position)})),groups=new Map();for(const r of rows)for(const cohort of ['all',r.fantasy_relevant?'fantasy_relevant':null].filter(Boolean)){const k=`${r.position}|${r.model}|${r.target_season}|${cohort}`,a=groups.get(k)||[];a.push({prediction:r.predicted_points,actual:r.actual_points});groups.set(k,a);}return{completeness:{total:c.length,complete:c.filter(x=>x.status==='complete_projection').length,partial:c.filter(x=>x.status==='partial_projection').length},metrics:[...groups.entries()].map(([k,a])=>{const[p,m,s,c]=k.split('|');return{position:p,model:m,target_season:+s,cohort:c,...metrics(a)};})};}
function uncertainty(pred){const m=new Map();for(const r of pred){const k=`${r.position}|${r.target}|${r.model}`,a=m.get(k)||[];a.push(r.actual-r.prediction);m.set(k,a);}return[...m.entries()].map(([k,a])=>{const[p,t,model]=k.split('|'),bands={};for(const level of[.5,.8,.9]){const lo=(1-level)/2,hi=1-lo,L=q(a,lo),H=q(a,hi),coverage=a.filter(x=>x>=L&&x<=H).length/a.length;bands[Math.round(level*100)]={low_residual:L,high_residual:H,observed_coverage:coverage};}return{position:p,target:t,model,n:a.length,bands};});}
function stability(playerSeasons){const idx=index(playerSeasons),targets=['total_tackles','solo_tackles','assisted_tackles','sacks','qb_hits','tackles_for_loss','interceptions','passes_defended','forced_fumbles'],out=[];for(const p of['DL','LB','DB'])for(const t of targets){const a=[],b=[];for(const r of playerSeasons.filter(x=>x.position===p)){const prev=history(idx,r.gsis_id,r.season,1)[0];if(prev&&Number.isFinite(prev.totals[t])&&Number.isFinite(r.totals[t])){a.push(prev.totals[t]);b.push(r.totals[t]);}}const c=corr(a,b),ma=mean(a.map((x,i)=>Math.abs(x-b[i]))),cv=mean(a.map((x,i)=>{const z=(Math.abs(x)+Math.abs(b[i]))/2;return z?Math.abs(x-b[i])/z:null;}).filter(Number.isFinite));let cls='extremely_noisy';if(Number.isFinite(c)&&c>=.6)cls='relatively_stable';else if(Number.isFinite(c)&&c>=.35)cls='moderately_predictable';else if(Number.isFinite(c)&&c>=.15)cls='high_variance';out.push({position:p,target:t,n:a.length,year_over_year_correlation:c,mae:ma,coefficient_of_variation:cv,classification:cls});}return out;}
function registry(metricRows){const all=metricRows.filter(r=>r.cohort==='all'),g=new Map();for(const r of all){const k=`${r.position}|${r.target}`,a=g.get(k)||[];a.push(r);g.set(k,a);}const out=[];for(const[k,rows]of g){const by=new Map();for(const r of rows){const a=by.get(r.model)||[];a.push(r);by.set(r.model,a);}const avg=[...by.entries()].map(([model,a])=>({model,folds:a.length,mae:mean(a.map(x=>x.mae)),rmse:mean(a.map(x=>x.rmse)),spearman:mean(a.map(x=>x.spearman).filter(Number.isFinite))})).sort((a,b)=>a.mae-b.mae),base=avg.find(x=>x.model==='weighted_603010'),best=avg[0],improvement=base&&best?(base.mae-best.mae)/base.mae:null,consistent=best?rows.filter(r=>r.model===best.model).filter(r=>{const b=rows.find(x=>x.model==='weighted_603010'&&x.target_season===r.target_season);return b&&r.mae<b.mae;}).length:0,promoted=best&&best.model!=='weighted_603010'&&improvement>=.02&&consistent>=3;const[p,t]=k.split('|');out.push({position:p,target:t,candidates:avg,preferred_experimental:promoted?best.model:'weighted_603010',meaningful_improvement:promoted,improvement_vs_weighted:improvement,folds_beating_weighted:consistent,experimental:true,production_projection_eligible:false,dynasty_value_eligible:false,reason:promoted?'Candidate beat weighted baseline by >=2% MAE and in >=3 folds.':'No challenger met the v0.2 promotion threshold; retain weighted baseline.'});}return out;}
function readiness(currentSleeper,crosswalk,playerSeasons,latestSeason=2024){const byGsis=new Map();for(const r of playerSeasons){const a=byGsis.get(r.gsis_id)||[];a.push(r);byGsis.set(r.gsis_id,a);}const counts={projection_ready:0,rookie_model_required:0,insufficient_history:0,identity_unresolved:0,unsupported_position:0};for(const[id,p]of Object.entries(currentSleeper||{})){const map=crosswalk?.mappings?.[id],pos=(p.position||p.fantasy_positions?.[0]||'').toUpperCase();if(!POSITIONS.includes(pos)){counts.unsupported_position++;continue;}if(!map?.gsis_id){counts.identity_unresolved++;continue;}const h=(byGsis.get(map.gsis_id)||[]).filter(x=>x.season<=latestSeason);if(!h.length){counts.rookie_model_required++;continue;}if(h.length<1){counts.insufficient_history++;continue;}counts.projection_ready++;}return counts;}
return{VERSION,POSITIONS,TARGETS,SCORING,REQUIRED,ageOn,aggregatePlayerSeasons,cohortThresholds,fitRidge,buildPredictions,metrics,groupMetrics,completeness,score,fantasyBenchmark,uncertainty,stability,registry,readiness};});