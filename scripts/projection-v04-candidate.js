#!/usr/bin/env node
'use strict';
const fs=require('node:fs'),path=require('node:path');const Core=require('../core-v08.js'),Data=require('../football-data-v08.js'),B=require('../projection-benchmark-v08.js'),Ridge=require('../ridge-v02.js'),P=require('../projection-v03.js'),Complete=require('../projection-v03-complete.js'),V1=require('./benchmark-projections.js'),H=require('./ingest-historical-data.js');
const ZERO=['attempts','completions','passing_yards','passing_td','interceptions','sacks','sack_yards','carries','rushing_yards','rushing_td','rushing_fumbles','targets','receptions','receiving_yards','receiving_td','air_yards','yards_after_catch'];const OL=V1.load;V1.load=async(...a)=>{const x=await OL(...a);for(const r of x.observations)if(['QB','RB','WR','TE'].includes(r.position_group))for(const f of ZERO)if(r.stats?.[f]?.state!=='value')r.stats[f]={state:'value',value:0};return x;};Complete.install();
const POS=['QB','RB','WR','TE'],F=[2020,2021,2022,2023,2024,2025],DEV=[2020,2021,2022,2023,2024],A=[1,10,100],mean=a=>a.length?a.reduce((s,x)=>s+x,0)/a.length:null;function rank(v){const o=v.map((x,i)=>({x,i})).sort((a,b)=>a.x-b.x),r=[];let i=0;while(i<o.length){let j=i;while(j+1<o.length&&o[j+1].x===o[i].x)j++;const q=(i+j+2)/2;for(let k=i;k<=j;k++)r[o[k].i]=q;i=j+1;}return r;}function cor(a,b){if(a.length<2)return null;const ma=mean(a),mb=mean(b);let n=0,da=0,db=0;for(let i=0;i<a.length;i++){const x=a[i]-ma,y=b[i]-mb;n+=x*y;da+=x*x;db+=y*y;}return da&&db?n/Math.sqrt(da*db):null;}function M(r){if(!r.length)return{n:0,mae:null,rmse:null,spearman:null,bias:null};const e=r.map(x=>x.p-x.a);return{n:r.length,mae:mean(e.map(Math.abs)),rmse:Math.sqrt(mean(e.map(x=>x*x))),spearman:cor(rank(r.map(x=>x.a)),rank(r.map(x=>x.p))),bias:mean(e)};}function wt(h,t){let n=0,d=0,w=[.6,.3,.1];for(let i=0;i<Math.min(3,h.length);i++){const v=h[i]?.totals?.[t];if(Number.isFinite(v)){n+=v*w[i];d+=w[i];}}return d?n/d:null;}function ratio(a,b){return Number.isFinite(a)&&Number.isFinite(b)&&b>0?a/b:null;}
function feat(h,t,pos,season,birth){if(!h[0]||!Number.isFinite(h[0].totals[t]))return null;const z=h[0].totals,l=[0,1,2].map(i=>h[i]?.totals?.[t]??null),pg=[0,1,2].map(i=>h[i]?.per_game?.[t]??null),g=[0,1,2].map(i=>h[i]?.games??null),age=Ridge.ageOn(birth,season);let opp=pos==='RB'?[z.carries??null,z.targets??null,z.receptions??null,ratio(z.rushing_yards,z.carries),ratio(z.receiving_yards,z.targets),ratio(z.receptions,z.targets),ratio(z.rushing_td,z.carries)]:pos==='WR'?[z.targets??null,z.receptions??null,z.carries??null,ratio(z.receptions,z.targets),ratio(z.receiving_yards,z.targets),ratio(z.receiving_td,z.targets)]:[];const hist=[...l,...pg,...g];if(pos==='RB'||pos==='WR')return[...hist,age,...opp];if(pos==='TE')return[...hist,age];return hist;}
function train(rows,idx,pos,t,before,birth){const X=[],y=[];for(const r of rows)if(r.position===pos&&r.season<before&&Number.isFinite(r.totals[t])){const f=feat(P.history(idx,r.gsis_id,r.season),t,pos,r.season,birth[r.gsis_id]);if(f){X.push(f);y.push(r.totals[t]);}}return{X,y};}function tune(rows,idx,pos,t,target,birth){const val=target-1,tr=train(rows,idx,pos,t,val,birth),act=rows.filter(r=>r.position===pos&&r.season===val&&Number.isFinite(r.totals[t]));if(tr.X.length<30)return 10;let best={a:10,mae:Infinity};for(const a of A){const m=Ridge.fit(tr.X,tr.y,a),q=[];if(!m)continue;for(const r of act){const f=feat(P.history(idx,r.gsis_id,val),t,pos,val,birth[r.gsis_id]);if(f)q.push({p:m.predict(f),a:r.totals[t]});}const s=M(q);if(Number.isFinite(s.mae)&&s.mae<best.mae)best={a,mae:s.mae};}return best.a;}
function fold(rows,y,birth){const idx=P.index(rows),act=rows.filter(r=>r.season===y&&POS.includes(r.position)),out=[],cache=new Map();for(const r of act){const h=P.history(idx,r.gsis_id,y);if(!h.length)continue;for(const t of P.TARGETS[r.position]||[]){if(!Number.isFinite(r.totals[t]))continue;const base=wt(h,t);if(!Number.isFinite(base))continue;out.push({id:r.gsis_id,pos:r.position,t,y,m:'baseline',p:base,a:r.totals[t],hc:h.length,age:Ridge.ageOn(birth[r.gsis_id],y)});if(r.position==='QB')continue;const k=`${r.position}|${t}`;if(!cache.has(k)){const alpha=tune(rows,idx,r.position,t,y,birth),tr=train(rows,idx,r.position,t,y,birth);cache.set(k,{alpha,model:Ridge.fit(tr.X,tr.y,alpha)});}const c=cache.get(k),f=feat(h,t,r.position,y,birth[r.gsis_id]);if(!c.model||!f)continue;const rp=c.model.predict(f);for(const [m,w] of [['ridge',1],['ens25',.25],['ens50',.5],['ens75',.75]])out.push({id:r.gsis_id,pos:r.position,t,y,m,p:m==='ridge'?rp:(1-w)*base+w*rp,a:r.totals[t],hc:h.length,age:Ridge.ageOn(birth[r.gsis_id],y)});}}return out;}
function choose(all,pos,t,y){if(pos==='QB')return'baseline';const prior=DEV.filter(z=>z<y);if(!prior.length)return'baseline';const br=all.filter(r=>r.pos===pos&&r.t===t&&r.m==='baseline'&&prior.includes(r.y)),b=M(br.map(r=>({p:r.p,a:r.a})));let best={m:'baseline',mae:b.mae};for(const m of ['ridge','ens25','ens50','ens75']){const q=all.filter(r=>r.pos===pos&&r.t===t&&r.m===m&&prior.includes(r.y)),s=M(q.map(r=>({p:r.p,a:r.a})));if(!Number.isFinite(s.mae))continue;const wins=prior.filter(z=>{const a=M(q.filter(r=>r.y===z).map(r=>({p:r.p,a:r.a}))),bb=M(br.filter(r=>r.y===z).map(r=>({p:r.p,a:r.a})));return Number.isFinite(a.mae)&&Number.isFinite(bb.mae)&&a.mae<bb.mae;}).length,imp=100*(b.mae-s.mae)/b.mae;if(s.mae<best.mae&&wins>=Math.ceil(prior.length/2)&&imp>=.5)best={m,mae:s.mae};}return best.m;}function selected(all,baselineOnly=false){const out=[];for(const y of F)for(const pos of POS)for(const t of P.TARGETS[pos]||[]){const m=baselineOnly?'baseline':choose(all,pos,t,y);out.push(...all.filter(r=>r.pos===pos&&r.t===t&&r.y===y&&r.m===m));}return out;}function fantasy(sel){const g=new Map();for(const r of sel){const k=`${r.id}|${r.pos}|${r.y}`,x=g.get(k)||{id:r.id,pos:r.pos,y:r.y,p:{},a:{},hc:r.hc,age:r.age};x.p[r.t]=Math.max(0,r.p);x.a[r.t]=r.a;g.set(k,x);}const out=[];for(const x of g.values())if((P.REQUIRED[x.pos]||[]).every(k=>Number.isFinite(x.p[k])&&Number.isFinite(x.a[k])))out.push({...x,pred:P.scoreReference(x.p,x.pos),act:P.scoreReference(x.a,x.pos)});return out;}function summary(rows){const out=[];for(const pos of POS)for(const y of F){const x=rows.filter(r=>r.pos===pos&&r.y===y).map(r=>({p:r.pred,a:r.act}));if(x.length)out.push({position:pos,season:y,...M(x)});}return out;}function topN(rows){const out=[],lim={QB:[12,24],RB:[12,24,36],WR:[12,24,36,48],TE:[12,24]};for(const pos of POS)for(const y of F){const d=rows.filter(r=>r.pos===pos&&r.y===y);if(!d.length)continue;const row={position:pos,season:y};for(const n of lim[pos])if(d.length>=n){const a=new Set([...d].sort((x,z)=>z.act-x.act).slice(0,n).map(r=>r.id)),p=[...d].sort((x,z)=>z.pred-x.pred).slice(0,n);row[`top${n}`]=p.filter(r=>a.has(r.id)).length;}out.push(row);}return out;}
async function main(){const dir=path.resolve(process.argv[2]||'data/reports/projection-v04-candidate');fs.mkdirSync(dir,{recursive:true});const seasons=Array.from({length:11},(_,i)=>2015+i),load=await V1.load(seasons,{cacheDir:'.cache/lv-v04-candidate',refresh:true}),pcsv=(await H.fetchText(Data.nflverseUrls(2015).players,'.cache/lv-v04-candidate/players.csv',{refresh:true,timeoutMs:45000})).text,players=Core.parseCsv(pcsv).map(Data.normalizeNflversePlayer),birth={};for(const p of players)if(p.identity.gsis_id)birth[p.identity.gsis_id]=p.bio.birth_date;const ps=B.aggregatePlayerSeasons(load.observations),all=[];for(const y of F)all.push(...fold(ps.filter(r=>r.season<=y),y,birth));const base=fantasy(selected(all,true)),cand=fantasy(selected(all,false)),result={version:'lv-projection-system-v0.4-candidate-research',experimental:true,production_projection_eligible:false,dynasty_value_eligible:false,development_folds:DEV,final_holdout:2025,feature_policy:{QB:'transparent 60/30/10 baseline',RB:'history + age + opportunity/efficiency Ridge ensembles',WR:'history + age + opportunity/efficiency Ridge ensembles',TE:'history + age Ridge ensembles'},selection_gate:'prior outer folds only; >=0.5% mean MAE gain + majority fold wins; otherwise baseline',baseline:summary(base),candidate:summary(cand),baseline_top_n:topN(base),candidate_top_n:topN(cand),selected_2025:Object.fromEntries(POS.map(pos=>[pos,Object.fromEntries((P.TARGETS[pos]||[]).map(t=>[t,choose(all,pos,t,2025)]))]))};fs.writeFileSync(path.join(dir,'summary.json'),JSON.stringify(result,null,2)+'\n');fs.writeFileSync(path.join(dir,'baseline-player-seasons.json'),JSON.stringify(base,null,2)+'\n');fs.writeFileSync(path.join(dir,'candidate-player-seasons.json'),JSON.stringify(cand,null,2)+'\n');console.log(JSON.stringify({final_baseline:result.baseline.filter(x=>x.season===2025),final_candidate:result.candidate.filter(x=>x.season===2025),selected:result.selected_2025},null,2));}
main().catch(e=>{console.error(e.stack||e);process.exit(1);});
