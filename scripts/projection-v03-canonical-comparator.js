#!/usr/bin/env node
'use strict';
const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto');
const Core=require('../core-v08.js'),Data=require('../football-data-v08.js'),B=require('../projection-benchmark-v08.js'),P=require('../projection-v03.js'),Complete=require('../projection-v03-complete.js'),V1=require('./benchmark-projections.js');
Complete.install();
const POS=['QB','RB','WR','TE'],F=[2020,2021,2022,2023,2024,2025],DEV=[2020,2021,2022,2023,2024],mean=a=>a.length?a.reduce((s,x)=>s+x,0)/a.length:null,sha=s=>crypto.createHash('sha256').update(s).digest('hex');
function rank(v){const o=v.map((x,i)=>({x,i})).sort((a,b)=>a.x-b.x),r=[];let i=0;while(i<o.length){let j=i;while(j+1<o.length&&o[j+1].x===o[i].x)j++;const q=(i+j+2)/2;for(let k=i;k<=j;k++)r[o[k].i]=q;i=j+1;}return r;}function cor(a,b){if(a.length<2)return null;const ma=mean(a),mb=mean(b);let n=0,da=0,db=0;for(let i=0;i<a.length;i++){const x=a[i]-ma,y=b[i]-mb;n+=x*y;da+=x*x;db+=y*y;}return da&&db?n/Math.sqrt(da*db):null;}function M(r){if(!r.length)return{n:0,mae:null,rmse:null,spearman:null,bias:null};const e=r.map(x=>x.p-x.a);return{n:r.length,mae:mean(e.map(Math.abs)),rmse:Math.sqrt(mean(e.map(x=>x*x))),spearman:cor(rank(r.map(x=>x.a)),rank(r.map(x=>x.p))),bias:mean(e)};}
function choose(all,pos,t,y){const prior=DEV.filter(z=>z<y);if(!prior.length)return'weighted_603010';const br=all.filter(r=>r.position===pos&&r.target===t&&r.model==='weighted_603010'&&prior.includes(r.target_season)),b=M(br.map(r=>({p:r.prediction,a:r.actual})));let best={m:'weighted_603010',mae:b.mae};for(const m of [...new Set(all.filter(r=>r.position===pos&&r.target===t).map(r=>r.model))].filter(x=>x!=='weighted_603010')){const q=all.filter(r=>r.position===pos&&r.target===t&&r.model===m&&prior.includes(r.target_season)),s=M(q.map(r=>({p:r.prediction,a:r.actual})));if(!Number.isFinite(s.mae))continue;const wins=prior.filter(z=>{const a=M(q.filter(r=>r.target_season===z).map(r=>({p:r.prediction,a:r.actual}))),bb=M(br.filter(r=>r.target_season===z).map(r=>({p:r.prediction,a:r.actual})));return Number.isFinite(a.mae)&&Number.isFinite(bb.mae)&&a.mae<bb.mae;}).length,imp=100*(b.mae-s.mae)/b.mae;if(imp>=2&&wins>=Math.ceil(prior.length/2)&&s.mae<best.mae)best={m,mae:s.mae};}return best.m;}
function selected(all){const out=[];for(const y of F)for(const pos of POS)for(const t of P.TARGETS[pos]||[]){const m=choose(all,pos,t,y);out.push(...all.filter(r=>r.position===pos&&r.target===t&&r.target_season===y&&r.model===m).map(r=>({...r,selected_model:m})));}return out;}
function fantasy(sel){const g=new Map();for(const r of sel){const k=`${r.gsis_id}|${r.position}|${r.target_season}`,x=g.get(k)||{id:r.gsis_id,pos:r.position,y:r.target_season,p:{},a:{}};x.p[r.target]=Math.max(0,r.prediction);x.a[r.target]=r.actual;g.set(k,x);}const out=[];for(const x of g.values())if((P.REQUIRED[x.pos]||[]).every(k=>Number.isFinite(x.p[k])&&Number.isFinite(x.a[k])))out.push({...x,pred:P.scoreReference(x.p,x.pos),act:P.scoreReference(x.a,x.pos)});return out.sort((a,b)=>a.y-b.y||a.pos.localeCompare(b.pos)||a.id.localeCompare(b.id));}
function summary(rows){const out=[];for(const pos of POS)for(const y of F){const x=rows.filter(r=>r.pos===pos&&r.y===y).map(r=>({p:r.pred,a:r.act}));if(x.length)out.push({position:pos,season:y,...M(x)});}return out;}
(async()=>{const out=path.resolve(process.argv[2]||'data/reports/projection-v04-canonical/v03'),cache=path.resolve(process.argv[3]||'.cache/lv-v04-canonical');fs.mkdirSync(out,{recursive:true});const manifest=JSON.parse(fs.readFileSync(path.join(cache,'snapshot-manifest.json'),'utf8'));for(const f of manifest.files){const text=fs.readFileSync(path.join(cache,f.path),'utf8');if(sha(text)!==f.sha256)throw new Error(`snapshot mismatch ${f.path}`);}const load=await V1.load(manifest.seasons,{cacheDir:cache,refresh:false}),players=Core.parseCsv(fs.readFileSync(path.join(cache,'players.csv'),'utf8')).map(Data.normalizeNflversePlayer),birth={};for(const p of players)if(p.identity.gsis_id)birth[p.identity.gsis_id]=p.bio.birth_date;const ps=B.aggregatePlayerSeasons(load.observations),all=[];for(const y of F)all.push(...P.predictFold(ps.filter(r=>r.season<=y),y,birth));if(all.some(r=>(r.history_seasons||[]).some(s=>s>=r.target_season)))throw new Error('temporal leakage');const rows=fantasy(selected(all)),result={version:'lv-v03-selection-safe-frozen-comparator',input_snapshot_sha256:manifest.snapshot_sha256,development_selection_seasons:DEV,retrospective_observed_season:2025,holdout_claim:false,selection:'original >=2% target MAE gate + majority prior-fold wins; earlier outer folds only',metrics:summary(rows),selected_for_2025:Object.fromEntries(POS.map(pos=>[pos,Object.fromEntries((P.TARGETS[pos]||[]).map(t=>[t,choose(all,pos,t,2025)]))]))};fs.writeFileSync(path.join(out,'summary.json'),JSON.stringify(result,null,2)+'\n');fs.writeFileSync(path.join(out,'player-seasons.json'),JSON.stringify(rows,null,2)+'\n');console.log(JSON.stringify(result.metrics.filter(x=>x.season===2025),null,2));})().catch(e=>{console.error(e.stack||e);process.exit(1);});
