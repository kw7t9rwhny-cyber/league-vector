(function(){'use strict';const $=id=>document.getElementById(id);function cat(k){k=String(k||'');if(/^(pass_|bonus_pass)/.test(k))return'Passing';if(/^(rush_|bonus_rush)/.test(k))return'Rushing';if(/^(rec|bonus_rec)/.test(k))return'Receiving';if(/(?:^|_)(?:tkl|tkl_solo|tkl_ast|tkl_loss|ast)(?:_|$)/.test(k))return'IDP tackles';if(/(?:sack|qb_hit)/.test(k))return'IDP sacks / pressure';if(/(?:^idp_|^)(?:int|ff|fum_rec|def_td|safe|pass_def)/.test(k)||/(?:_int|_ff|_fum_rec|_def_td|_safe|_pass_def)/.test(k))return'IDP turnovers / plays';if(/^(fg|xpm|kick)/.test(k))return'Kicking';if(/(?:kr|pr)_yd|return/.test(k))return'Returns';if(/pts_allow|def_st|st_/.test(k))return'Team defense / special teams';if(/fum/.test(k))return'Fumbles';return'Other bonuses / settings';}function split(v){return String(v||'').split(',').map(x=>x.trim()).filter(x=>x&&x!=='none'&&x!=='none detected');}function parse(text){text=String(text||'');let a=text.match(/Legacy applied:\s*([\s\S]*?)(?:Legacy unsupported\/non-matching:|$)/i),u=text.match(/Legacy unsupported\/non-matching:\s*([\s\S]*)$/i);if(!a&&!u){a=text.match(/Applied keys:\s*([\s\S]*?)(?:Unsupported\/non-matching keys:|$)/i);u=text.match(/Unsupported\/non-matching keys:\s*([\s\S]*)$/i);}return{applied:split(a?.[1]),unsupported:split(u?.[1])};}function statuses(applied,unsupported){const m=new Map();for(const [keys,type] of [[applied,'s'],[unsupported,'u']])for(const k of keys){const c=cat(k),r=m.get(c)||{s:0,u:0};r[type]++;m.set(c,r);}const o={full:[],partial:[],unsupported:[]};for(const [c,r] of m){if(r.s&&!r.u)o.full.push(c);else if(r.s&&r.u)o.partial.push(c);else o.unsupported.push(c);}for(const v of Object.values(o))v.sort();return o;}const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const list=a=>a.length?a.join(', '):'none';function applyLegacy(){const el=$('scoringCoverage');if(!el||el.dataset.coverageV032==='1')return false;const p=parse(el.textContent);if(!p.applied.length&&!p.unsupported.length)return false;const n=p.applied.length+p.unsupported.length,c=n?Math.round(p.applied.length/n*100):100,s=statuses(p.applied,p.unsupported);el.innerHTML=`<b>Legacy scoring active-key coverage:</b> ${c}% <span class="source-note">(${p.applied.length}/${n} active keys)</span><br><b>Fully supported:</b> ${esc(list(s.full))}<br><b>Partially supported:</b> ${esc(list(s.partial))}<br><b>Unsupported:</b> ${esc(list(s.unsupported))}<div id="v03ScoringCoverageSummary"></div><details><summary>Show technical scoring keys</summary><p><b>Legacy applied:</b> ${esc(list(p.applied))}<br><b>Legacy unsupported/non-matching:</b> ${esc(list(p.unsupported))}</p></details>`;el.dataset.coverageV032='1';return true;}function applyV03(scoring){applyLegacy();const PF=window.LeagueVectorProjectionFrontend,host=$('v03ScoringCoverageSummary');if(!PF||!host)return null;const active=Object.entries(scoring||{}).filter(([,v])=>Number.isFinite(Number(v))&&Number(v)!==0),supported=[],unsupported=[];for(const [k]of active){if(PF.BASE_KEYS?.[k]||k==='bonus_rec_te'||k==='rec_te')supported.push(k);else unsupported.push(k);}const n=active.length,pct=n?Math.round(supported.length/n*100):100,s=statuses(supported,unsupported);host.innerHTML=`<hr><b>League Vector v0.3 active-key coverage:</b> ${pct}% <span class="source-note">(${supported.length}/${n} active keys)</span><br><b>Fully supported:</b> ${esc(list(s.full))}<br><b>Partially supported:</b> ${esc(list(s.partial))}<br><b>Unsupported:</b> ${esc(list(s.unsupported))}`;return{method:'active-key-count',active_keys:n,supported_keys:supported.length,coverage_pct:pct,full:s.full,partial:s.partial,unsupported:s.unsupported};}const observer=new MutationObserver(()=>applyLegacy());observer.observe(document.documentElement,{childList:true,subtree:true,characterData:true});window.LeagueVectorScoringCoverage={categoryForKey:cat,statuses,parse,applyLegacy,applyV03};})();
