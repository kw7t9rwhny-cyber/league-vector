(function(){'use strict';
const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const debug=new URLSearchParams(location.search).get('debug')==='1';
function current(){return window.__leagueVectorExperimental?.rows||[];}
function sortRows(rows){const mode=$('projectionSort')?.value||'points',confidence={High:3,Medium:2,Low:1};return [...rows].sort((a,b)=>{if(mode==='name')return a.name.localeCompare(b.name);if(mode==='status')return String(a.projection_status).localeCompare(String(b.projection_status))||a.name.localeCompare(b.name);if(mode==='confidence')return (confidence[b.record?.confidence]||0)-(confidence[a.record?.confidence]||0)||a.name.localeCompare(b.name);if(mode==='rank'){const ar=Number(String(a.projected_position_rank||'').replace(/\D/g,''))||9999,br=Number(String(b.projected_position_rank||'').replace(/\D/g,''))||9999;return ar-br||a.name.localeCompare(b.name);}const ap=a.scoring?.completeness==='complete'?a.scoring.points:-Infinity,bp=b.scoring?.completeness==='complete'?b.scoring.points:-Infinity;return bp-ap||a.name.localeCompare(b.name);});}
function applyFilters(){const rows=current(),query=($('projectionSearch')?.value||'').trim().toLowerCase(),pos=$('projectionPosition')?.value||'ALL',team=$('projectionTeam')?.value||'ALL',cards=[...document.querySelectorAll('#experimentalProjectionRows .projection-card')];if(!rows.length||cards.length!==rows.length)return;const ordered=sortRows(rows.filter(r=>(pos==='ALL'||r.position===pos)&&(team==='ALL'||String(r.roster_id)===team)&&(!query||r.name.toLowerCase().includes(query))));const byName=new Map(cards.map(card=>[card.querySelector('b')?.textContent,card]));const target=$('experimentalProjectionRows');for(const row of ordered){const card=byName.get(row.name);if(card){card.hidden=false;target.append(card);}}for(const card of cards)if(!ordered.some(r=>r.name===card.querySelector('b')?.textContent))card.hidden=true;}
function attachTeams(){const exp=window.__leagueVectorExperimental;if(!exp?.rows)return;const rosterCards=[...document.querySelectorAll('#teams .team')],teamNames=rosterCards.map(x=>x.querySelector('h3')?.textContent||'');const select=$('projectionTeam');if(select&&select.options.length<=1){teamNames.forEach((name,i)=>select.add(new Option(name,String(i+1))));}
for(const row of exp.rows){const card=rosterCards.find(c=>[...c.querySelectorAll('.player-name')].some(n=>n.textContent===row.name));if(card){row.fantasy_team=card.querySelector('h3')?.textContent||null;row.roster_id=rosterCards.indexOf(card)+1;}}
}
function diagnostics(){if(!debug)return;const panel=$('projectionDiagnostics'),target=$('projectionDiagnosticsRows'),exp=window.__leagueVectorExperimental;if(!panel||!target||!exp?.rows)return;panel.hidden=false;target.innerHTML=exp.rows.map(r=>`<details><summary>${esc(r.name)} — ${esc(r.projection_status)}</summary><pre>${esc(JSON.stringify({sleeper_id:r.sleeper_id,gsis_id:r.gsis_id||r.record?.gsis_id||null,league_vector_player_id:r.league_vector_player_id||r.record?.league_vector_player_id||null,identity_method:r.identity_method||null,projection_status:r.projection_status,models:r.record?.models||{},missing_inputs:r.record?.missing_inputs||[],scoring_coverage:r.scoring?.coverage_pct??null,scoring_completeness:r.scoring?.completeness??null,unsupported_scoring:r.scoring?.unsupported_keys||[],warnings:[...(r.record?.warnings||[]),...(r.identity_warning?[r.identity_warning]:[])]},null,2))}</pre></details>`).join('');}
function improveTeamRanks(){const cards=[...document.querySelectorAll('#experimentalTeamProjection .projection-team')];if(!cards.length)return;const complete=cards.filter(c=>{const t=c.querySelector('small')?.textContent||'',m=t.match(/(\d+)\/(\d+)/);return m&&m[1]===m[2]&&Number(m[2])>0;}).sort((a,b)=>Number(b.querySelector('strong')?.textContent||0)-Number(a.querySelector('strong')?.textContent||0));const rank=new Map(complete.map((c,i)=>[c,i+1]));for(const card of cards){const first=card.querySelector('span');if(first)first.textContent=rank.has(card)?`#${rank.get(card)}`:'Unranked';}}
function enhance(){if(!window.__leagueVectorExperimental?.rows)return false;attachTeams();diagnostics();improveTeamRanks();for(const id of ['projectionSearch','projectionPosition','projectionTeam','projectionSort']){const el=$(id);if(el&&!el.dataset.lvEnhanced){el.dataset.lvEnhanced='1';el.addEventListener('input',applyFilters);el.addEventListener('change',applyFilters);}}return true;}
$('go')?.addEventListener('click',()=>{let tries=0;const timer=setInterval(()=>{tries++;if(enhance()||tries>100)clearInterval(timer);},50);});
})();
